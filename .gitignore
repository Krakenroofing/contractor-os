node_modules
.next
.env
.env.local
*.log
.DS_Store
drizzle/meta
.vercel
.turbo

# Capacitor native shells (Phase M5) — generated locally per-machine by
# `npx cap add ios` / `npx cap add android`. Not committed because the
# shells are huge and regenerable from capacitor.config.ts + npm deps.
# See SETUP-CAPACITOR.md.
ios/
android/
